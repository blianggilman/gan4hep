# to push to this fork
```
make changes on local repo
git add .
git commit -m "message"
git push beaFork main:main
```

# to work on this on another computer
```
git clone
git add .
git commit -m "message"
git push
```

# gan4hep
Use GAN to generate HEP events

# Installation
```
pip install -e . 
```

